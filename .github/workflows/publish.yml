name: Publish to Hex.pm

on:
  push:
    tags:
      - 'v*.*.*'
      - 'v*.*.*-rc.*'
      - 'v*.*.*-beta.*'
      - 'v*.*.*-alpha.*'

env:
  MIX_ENV: prod
  ELIXIR_VERSION: "1.20"
  OTP_VERSION: "29"

jobs:
  publish:
    name: Publish to Hex.pm
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Setup Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ env.ELIXIR_VERSION }}
          otp-version: ${{ env.OTP_VERSION }}

      - name: Cache dependencies
        uses: actions/cache@v6
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-mix-publish-${{ hashFiles('**/mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-mix-publish-

      - name: Install dependencies
        run: |
          mix deps.get --only prod
          MIX_ENV=dev mix deps.get --only dev

      - name: Compile project
        run: |
          mix compile
          MIX_ENV=dev mix compile

      - name: Run tests
        env:
          # The workflow-level MIX_ENV is prod, under which test/support is not
          # compiled, so any test that imports a support module fails to compile.
          MIX_ENV: test
        run: mix test --exclude slow --exclude omnetpp

      - name: Build documentation
        env:
          MIX_ENV: dev
        run: mix docs

      - name: Publish to Hex
        env:
          HEX_API_KEY: ${{ secrets.HEX_API_KEY }}
          MIX_ENV: dev
        run: |
          mix hex.publish --yes --replace

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          prerelease: ${{ contains(github.ref, '-rc.') || contains(github.ref, '-beta.') || contains(github.ref, '-alpha.') }}
          files: |
            CHANGELOG.md

